import bgVideos from '../bgvideos';
import graph01 from '../graph01';
import heroSquares from '../hero-squares';

export default function homepageScript() {
  bgVideos();
  heroSquares();
  graph01();
}
